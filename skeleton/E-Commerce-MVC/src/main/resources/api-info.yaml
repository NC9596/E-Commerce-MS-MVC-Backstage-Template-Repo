apiVersion: backstage.io/v1alpha1
kind: API
metadata:
  name: E-Commerce-MVC-MS
  description: ${{ values.description }}
spec:
  type: openapi
  lifecycle: experimental
  owner: ${{values.destination.owner}}
  definition:
    $text: https://github.com/${{values.destination.owner + "/" + values.destination.repo}}/blob/master/E-Commerce-MVC-MS/E-Commerce-MVC-MS/src/main/resources/api-docs.yaml
